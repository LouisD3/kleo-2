export default function GoogleClassroomIcon({ size = 20 }) {
  return (
    <img
      src="/google-classroom-logo.png"
      alt="Google Classroom"
      height={size}
      style={{ height: size, width: 'auto' }}
    />
  )
}
